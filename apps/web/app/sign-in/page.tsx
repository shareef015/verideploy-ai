export default function SignInPage(){return <main className="auth-page"><section className="auth-card"><p className="eyebrow">Authentication required</p><h1>Sign in through your VeriDeploy identity provider</h1><p>Production authentication uses OIDC Authorization Code with S256 PKCE. Tenant and role context come from the identity provider and are signed into the server-side VeriDeploy session.</p><a href="/api/auth/login">Continue with identity provider</a></section></main>}

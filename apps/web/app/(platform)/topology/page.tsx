"use client";
import { useMemo } from "react";
import { useQuery } from "@tanstack/react-query";
import { gatewayFetch } from "../../../lib/api/gateway-client";
import { TopologySnapshotSchema } from "../../../lib/schemas/topology";
import { useFrontendSession } from "../../../providers/session-provider";
import { StatePanel } from "../../../components/ui/state-panel";
export default function TopologyPage(){const session=useFrontendSession();const query=useQuery({queryKey:["topology","nexuspay",session.tenantId],queryFn:()=>gatewayFetch("/api/v1/topology/nexuspay",session,{},TopologySnapshotSchema)});const data=query.data;const teamById=useMemo(()=>new Map((data?.teams??[]).map(t=>[t.team_id,t])),[data]);const serviceById=useMemo(()=>new Map((data?.services??[]).map(s=>[s.service_id,s])),[data]);const ownerByTeam=useMemo(()=>new Map((data?.owners??[]).map(o=>[o.team_id,o])),[data]);const production=useMemo(()=>data?.environments.find(e=>e.name==="production"),[data]);if(query.isLoading)return <div className="page"><StatePanel title="Loading service topology" description="Resolving authorized NexusPay topology…"/></div>;if(query.error)return <div className="page"><StatePanel role="alert" title="Topology unavailable" description={query.error.message}/></div>;if(!data)return null;return <div className="page topologyPage"><section className="hero"><p className="eyebrow">Synthetic company model · {data.seed_version}</p><h1>{data.company.name} Service Topology</h1><p>Stable ownership, dependencies, production SLOs, environments, and deployed versions. Seed <code>{data.seed_sha256.slice(0,12)}</code>.</p></section><section className="topologyStats"><article><strong>{data.teams.length}</strong><span>Teams</span></article><article><strong>{data.services.length}</strong><span>Services</span></article><article><strong>{data.dependencies.length}</strong><span>Dependencies</span></article><article><strong>{data.deployments.length}</strong><span>Deployments</span></article></section><section className="serviceGrid">{data.services.map(service=>{const team=teamById.get(service.team_id),owner=ownerByTeam.get(service.team_id),prodDeploy=data.deployments.find(d=>d.service_id===service.service_id&&d.environment_id===production?.environment_id),availability=data.slos.find(s=>s.service_id===service.service_id&&s.metric==="availability");return <article className={`serviceCard ${service.tier}`} key={service.service_id}><div className="serviceHeader"><span>{service.domain}</span><b>{service.tier.replace("_"," ")}</b></div><h2>{service.name}</h2><p>{service.description}</p><dl><dt>Team</dt><dd>{team?.name}</dd><dt>Owner</dt><dd>{owner?.display_name}</dd><dt>Runtime</dt><dd>{service.runtime}</dd><dt>Prod SLO</dt><dd>{availability?.target}%</dd><dt>Deployment</dt><dd>{prodDeploy?.version}</dd></dl></article>})}</section><section className="card dependencyPanel"><p className="eyebrow">Directed dependency graph</p><h2>Critical service relationships</h2><div className="dependencyList">{data.dependencies.map(dep=><div key={dep.dependency_id}><strong>{serviceById.get(dep.source_service_id)?.name}</strong><span>→ {dep.kind.replace("_"," ")} · {dep.criticality} →</span><strong>{serviceById.get(dep.target_service_id)?.name}</strong></div>)}</div></section></div>}

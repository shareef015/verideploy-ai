"use client";
import { useQuery } from "@tanstack/react-query";
import { gatewayFetch } from "../../../lib/api/gateway-client";
import { EvidenceGraphSnapshotSchema } from "../../../lib/schemas/evidence-graph";
import { useFrontendSession } from "../../../providers/session-provider";
import { StatePanel } from "../../../components/ui/state-panel";
const order=["pull_request","service","incident","root_cause","evidence"];
export default function EvidenceGraphPage(){const session=useFrontendSession();const query=useQuery({queryKey:["evidence-graph",session.tenantId],queryFn:()=>gatewayFetch("/api/v1/evidence-graph/snapshot",session,{},EvidenceGraphSnapshotSchema)});if(query.isLoading)return <div className="page"><StatePanel title="Loading evidence graph" description="Retrieving authorized lineage…"/></div>;if(query.error)return <div className="page"><StatePanel role="alert" title="Evidence graph unavailable" description={query.error.message}/></div>;const data=query.data!;const byId=new Map(data.entities.map(e=>[e.entity_id,e]));const lanes=[...data.entities].sort((a,b)=>order.indexOf(a.entity_type)-order.indexOf(b.entity_type));return <div className="page graphPage"><section className="hero"><p className="eyebrow">Tenant-isolated relational graph</p><h1>Evidence Graph & Lineage</h1><p>Typed entities, temporal relationships, immutable evidence references, and queryable cause paths. Snapshot <code>{data.snapshot_sha256.slice(0,12)}</code>.</p></section><section className="graphStats"><article><strong>{data.entities.length}</strong><span>Entities</span></article><article><strong>{data.edges.length}</strong><span>Relationships</span></article></section><section className="graphFlow">{lanes.map((e,index)=><div className="graphNode" key={e.entity_id}><span>{e.entity_type.replaceAll("_"," ")}</span><strong>{e.label}</strong><small>{e.observed_at?new Date(e.observed_at).toLocaleString():"reference entity"}</small>{index<lanes.length-1&&<i>→</i>}</div>)}</section><section className="card graphEdges"><p className="eyebrow">Typed temporal edges</p><h2>Lineage and causal relationships</h2>{data.edges.map(e=><div key={e.edge_id}><strong>{byId.get(e.source_entity_id)?.label}</strong><span>{e.relationship.replaceAll("_"," ")} · {(e.confidence*100).toFixed(0)}%</span><strong>{byId.get(e.target_entity_id)?.label}</strong></div>)}</section></div>}

# AI-Engineering Job-Description Mapping

AI Engineering Job Description Mapping converts portfolio claims into a machine-verifiable evidence graph. `config/career/jd-mapping.json` is the canonical source; CI validates that every required skill has code plus verification evidence and that every referenced artifact exists and is non-empty. This prevents resume/JD claims from drifting beyond the implementation.
